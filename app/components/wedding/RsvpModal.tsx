import type { FormEvent } from "react";
import type { GuestSide } from "./constants";

type RsvpModalProps = { guestSide: GuestSide | null; shortLabel?: string; submitted: boolean; submitting: boolean; error: string | null; onClose: () => void; onSubmit: (event: FormEvent<HTMLFormElement>) => void };

export function RsvpModal({ guestSide, shortLabel, submitted, submitting, error, onClose, onSubmit }: RsvpModalProps) {
  return <div className="modal-backdrop" role="dialog" aria-modal="true" aria-labelledby="rsvp-title"><div className="rsvp-modal"><button className="modal-close" type="button" onClick={onClose} aria-label="Đóng">×</button>{!submitted ? <><p className="micro-title">RSVP</p><h2 id="rsvp-title">Xác nhận tham dự</h2><p className="modal-intro">Cảm ơn bạn đã dành thời gian hồi đáp lời mời của Nam &amp; Mai.</p><form onSubmit={onSubmit}><label>Họ và tên<input name="guestName" required placeholder="Tên của bạn" /></label><label>Bạn sẽ tham dự chứ?<select name="attendance" required defaultValue=""><option value="" disabled>Chọn phản hồi</option><option value="yes">Mình sẽ tham dự</option><option value="no">Rất tiếc, mình không thể đến</option></select></label><label>Số người tham dự<select name="guests" defaultValue="1"><option>1</option><option>2</option><option>3</option><option>4</option></select></label><div className="rsvp-guest-side"><span>Bạn là khách của</span><strong>{shortLabel}</strong></div><input type="hidden" name="family" value={guestSide ?? ""} /><label>Lời nhắn<textarea name="message" rows={3} placeholder="Gửi một lời chúc đến cô dâu chú rể..." /></label>{error && <p className="form-error" role="alert">{error}</p>}<button className="submit-button" type="submit" disabled={submitting}>{submitting ? "Đang gửi..." : "Gửi lời hồi đáp"}</button></form></> : <div className="thank-you"><span>✓</span><p className="micro-title">Cảm ơn bạn</p><h2>Phản hồi đã được ghi nhận</h2><p>Nam &amp; Mai rất mong được gặp bạn trong ngày trọng đại.</p><button type="button" onClick={onClose}>Đóng</button></div>}</div></div>;
}
